apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app.kubernetes.io/name: fluxa
data:
  ENV: {{ .Values.env.ENV | quote }}
  DATABASE_NAME: {{ .Values.env.DATABASE_NAME | quote }}
  TEMPORAL_HOST: {{ .Values.env.TEMPORAL_HOST | quote }}
